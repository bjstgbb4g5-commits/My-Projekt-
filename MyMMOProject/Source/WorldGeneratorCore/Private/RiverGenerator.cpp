#include "RiverGenerator.h"

TArray<TArray<FVector>> URiverGenerator::GenerateRivers(const TArray<FWorldRegion>& Regions, int32 GridSizeX, int32 GridSizeY, float RegionSize, FRandomStream& Random)
{
    TArray<TArray<FVector>> Rivers;
    // Заглушка: возвращаем пустой массив
    return Rivers;
}
